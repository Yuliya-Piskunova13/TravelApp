import { StyleSheet } from 'react-native';

import GLOBAL_FONTS from '../../assets/styles/globalFonts';

const styles = StyleSheet.create({
  text: GLOBAL_FONTS.bodySRegular,
});

export default styles;
